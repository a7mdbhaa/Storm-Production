import {describe,it,expect} from 'vitest';import {findLanguage,languages,languagesForFlag,searchLanguages} from '../src/config/languages.js';
describe('languages',()=>{it('contains over 100 languages',()=>expect(languages.length).toBeGreaterThan(100));it('resolves names, native names, ISO, variants and flags',()=>{expect(findLanguage('Arabic')?.code).toBe('ar');expect(findLanguage('العربية')?.code).toBe('ar');expect(findLanguage('fr')?.name).toBe('French');expect(findLanguage('Portuguese Brazil')?.code).toBe('pt-BR');expect(languagesForFlag('🇫🇷')).toContain('fr');});it('supports autocomplete and rejects invalid values',()=>{expect(searchLanguages('chin').length).toBeGreaterThan(1);expect(findLanguage('Klingon')).toBeUndefined();});});
